#include "main.h"
/**
 * add -Adds two integers and returns
 * the result.
 *
 * @a: number one.
 * <@b: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
